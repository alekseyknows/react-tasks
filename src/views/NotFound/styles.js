import { makeStyles } from '@material-ui/core';

const useStyles = makeStyles({
    content: {
        textAlign: 'center',
        marginTop: '200px',
        fontWeight: 'bold',
        fontSize: 50,
    },
});

export { useStyles };
